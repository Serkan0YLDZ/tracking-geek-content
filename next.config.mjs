/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['image.tmdb.org','covers.openlibrary.org', 'cdn.myanimelist.net', '*.*'],
    },
  };
  
  export default nextConfig;
  